import { Typography, Grid } from "@mui/material"

const Dashboard = () => {
    return (
        <Grid sx={{display:"flex", justifyContent:"center", alignItems:"center", height:"100vh", width:"1000vw"}}>
            <Typography variant="h3" component="p">Dashboard</Typography>
        </Grid>
    )
}

export default Dashboard